# Absolute path to the investment-vault repository root.
# Optional when the vault sits next to this repo as ../investment-vault.
VAULT_PATH=

# Optional SQLite path (defaults to ./data/dashboard.db)
# DATABASE_PATH=
